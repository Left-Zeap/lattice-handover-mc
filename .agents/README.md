# `.agents`：自动化协作预留目录

这个隐藏目录为代码助手或自动化协作流程预留。目前它不包含物理模型、
实验数据或程序运行所需配置。

普通使用者可以忽略本目录。运行

```powershell
python -m continuous_loading
```

或

```powershell
python -m pytest -q
```

都不依赖这里的内容。

如果以后加入自动化说明，应只放：

- 面向代码助手的仓库约定；
- 重复任务的工作流说明；
- 不含密钥的辅助配置。

不要在这里保存实验数据、访问令牌、个人路径或计算结果。
